当前任务：为 SQL Server 表 BS_Agent_DingDan 设计并实现一个 Jupyter Notebook，用于字段理解、数据探索性分析、初步清洗和清洗规则沉淀。不要继续推进 detector、前端或工单系统。

背景：

当前数据来自医药供应链订单表 BS_Agent_DingDan。数据样例字段包括：

数据唯一标识符、订单明细ID、数据来源、订单名称、省、省编码、市、市编码、县区、县区编码、采购时间、药品编码、药品医保编码、通用名、商品名、剂型、规格、转换系数、采购单位、材质、采购价(元)、采购数量、采购金额(元)、配送数量、配送金额(元)、到货数量、到货金额(元)、采购地址、订单状态、企业编码、医疗机构等级、医疗机构详细等级、医疗机构编码、医疗机构、配送企业编码、配送企业、生产企业编码、生产企业、药品类别、数据更新时间、配送时间、到货时间、项目名称、所有制形式、退回数量、作废数量。

请完成以下内容。

一、文件位置要求

1. 新建 notebook：

notebooks/01_BS_Agent_DingDan_EDA_and_Cleaning.ipynb

2. 新建人类可读字段字典：

docs/data_dictionary/BS_Agent_DingDan字段说明.md

3. 新建机器可读 schema 配置：

configs/data_schema/bs_agent_dingdan_schema.yaml

4. 新建映射配置目录：

configs/mappings/

至少包含：

configs/mappings/order_status_map.yaml

configs/mappings/hospital_grade_map.yaml

configs/mappings/drug_category_map.yaml

configs/mappings/ownership_map.yaml

5. Notebook 输出目录：

exports/eda/

exports/clean/

exports/mappings/

二、字段字典要求

请在 docs/data_dictionary/BS_Agent_DingDan字段说明.md 和 configs/data_schema/bs_agent_dingdan_schema.yaml 中保存字段含义。字段含义至少包括：

- 原始中文字段名

- 推荐英文别名

- 字段类型：identifier / metadata / geography / time / drug / institution / enterprise / numeric_sensitive / status / category

- 是否进入 clean 表

- 清洗规则

- 当前是否可用于算法

- 备注

注意：

1. 数据来源字段暂时认为有效但不进入算法。

2. 订单名称暂时无效，不进入算法。

3. 数据更新时间暂时忽略。

4. 配送时间和到货时间 null 太多，暂时不进入算法。

5. 项目名称大量 null，暂时不进入算法。

6. 采购地址大量 null，暂时不进入算法。

7. 剂型、规格、采购单位、转换系数、材质暂时不进入算法，但可保留在药品维度审计字段中。

8. 通用名就是药品具体名称，建议 clean 表中保留为 drug_name。

三、Notebook 分析步骤

Notebook 必须包含以下章节，并用 Markdown 说明每一步目的。

0. 环境与路径配置

- 读取 .env 中的 SQL_DATABASE_URL 和 SQL_TABLE。

- 支持从 SQL Server 读取，也支持如果 exports/raw/BS_Agent_DingDan_sample.csv 存在则直接读 CSV。

- SQL 查询默认 SELECT TOP，可通过参数控制 max_rows，避免一次读爆内存。

1. 基础规模检查

输出：

- 行数

- 列数

- 时间范围：采购时间 min/max

- 每列 null rate

- 每列 distinct count

保存到：

exports/eda/field_distinct_counts.csv

2. 唯一标识符检查

检查：

- 数据唯一标识符是否唯一

- 订单明细ID是否唯一

- 数据唯一标识符与订单明细ID是否一一对应

如果订单明细ID不唯一，继续分析：

- 同一个订单明细ID下有多少行

- 是否出现不同订单状态

- 是否出现不同数据更新时间

- 是否出现不同配送企业

- 是否出现不同采购数量/配送数量/到货数量

- 抽样输出重复组

保存：

exports/eda/id_duplicate_report.csv

exports/eda/order_detail_duplicate_samples.csv

结论不要武断。如果订单明细ID重复，标记为：

“可能代表同一个订单明细的多次状态/生命周期记录，需要进一步确认。”

3. 数据来源与订单名称

输出 distinct count 和 top value。

但在 clean 表中不使用这两列。

4. 地区字段清洗

说明：省编码、市编码、县区编码更准确地说是行政区划编码，不是邮编。

保留县区编码 county_code。

从县区编码派生：

- province_code = 前2位 + "0000"

- city_code = 前4位 + "00"

- county_code = 原县区编码

生成 region_code_map.csv，包含：

province_code, province_name, city_code, city_name, county_code, county_name

检查：

- 同一个 province_code 是否对应多个 province_name

- 同一个 city_code 是否对应多个 city_name

- 同一个 county_code 是否对应多个 county_name

保存：

exports/mappings/region_code_map.csv

exports/eda/region_code_conflicts.csv

clean 表中保留：

province_code, city_code, county_code

省、市、县名称可不进入主 clean 表，但映射表必须保存。

5. 药品编码与医保编码一致性

检查：

- 药品编码 == 药品医保编码 的比例

- 不一致样本

- 药品编码 -> 药品医保编码 是否一对一

保存：

exports/eda/drug_code_vs_insurance_code_report.csv

如果完全一致，则 clean 表只保留 drug_code，医保编码不进入算法字段。

6. 数值字段脱敏影响检查

敏感数值字段：

采购价(元)、采购数量、采购金额(元)、配送数量、配送金额(元)、到货数量、到货金额(元)

当前已知：这些字段可能分别乘了独立随机数。Notebook 必须明确标注：

“当前数值字段仅做脱敏破坏程度检查，不用于业务算法判断。”

检查：

- 采购金额 是否约等于 采购价 × 采购数量

- 配送数量 <= 采购数量 的比例

- 到货数量 <= 配送数量 的比例

- 各数值字段 null rate、负数比例、零值比例、分布摘要

保存：

exports/eda/numeric_desensitization_report.csv

不要基于这些字段做配送率、到货率、金额趋势、数量趋势算法结论。

7. 订单状态枚举与初步归类

输出所有订单状态 value_counts，保存：

exports/eda/status_value_counts.csv

建立初版状态归类，输出：

order_status_raw

order_status_stage

order_status_code

初始归类建议：

0 unknown

1 confirmed 已确认

2 dispatched 已配送/发货/配送确认

3 received 已收货/已入库

4 invoiced 已开发票

5 returned 退货/售后/退回

6 rejected_or_cancelled 拒绝/撤单/作废/驳回

要求：

- 用 configs/mappings/order_status_map.yaml 管理映射。

- 对未匹配状态输出到 exports/eda/status_unmapped_values.csv。

- 同时输出 exports/mappings/order_status_map_review.csv 方便人工检查。

8. 医疗机构等级清洗
此部分更新，后文具体解释方案，此处占位

9. 企业字段关系检查

检查字段：

企业编码、生产企业编码、生产企业

输出：

- 企业编码 distinct count

- 生产企业编码 distinct count

- 生产企业 distinct count

- 企业编码 -> 生产企业编码 是否一对一

- 生产企业编码 -> 生产企业 是否一对一

- 企业编码 -> 生产企业 是否一对一

保存：

exports/eda/enterprise_mapping_report.csv

如果三者全表都只有一个值，则在报告中标记为：

“可能是本企业数据口径下的冗余字段，clean 表可只保留 manufacturer_code 和 manufacturer_name；原始字段仍在 raw 层保留。”

不要在没有报告前直接删除。

10. 药品类别编码

药品类别是长文本，但预计取值少于 30。

输出：

- drug_category_raw

- drug_category_code

生成映射：

exports/mappings/drug_category_map.csv

configs/mappings/drug_category_map.yaml

11. 所有制形式编码

输出 value_counts。

生成：

- ownership_type_raw

- ownership_type_code

映射表：

exports/mappings/ownership_map.csv

configs/mappings/ownership_map.yaml

常见值包括：公立、民营、其他、未知。

12. 退回数量与作废数量

退回数量：

- null 视为 0

- 输出退回数量非零比例

作废数量：

- 输出非零行数和样本

- 如果非零极少，clean 表暂时不纳入算法字段，但报告中保留统计

保存：

exports/eda/return_void_quantity_report.csv

13. 生成 clean 表

clean 表建议字段包括：

标识：

- row_uid = 数据唯一标识符

- order_detail_id = 订单明细ID

时间：

- purchase_time = 采购时间

地区：

- province_code

- city_code

- county_code

药品：

- drug_code

- drug_name = 通用名

- product_name = 商品名，可选保留

- drug_category_code

- drug_category_raw

机构：

- hospital_code = 医疗机构编码

- hospital_name = 医疗机构

- hospital_level_num

- hospital_level_label

- hospital_class_code

- hospital_class_label

- ownership_type_code

- ownership_type_raw

配送企业：

- distributor_code = 配送企业编码

- distributor_name = 配送企业

生产企业：

- manufacturer_code = 生产企业编码

- manufacturer_name = 生产企业

状态：

- order_status_raw

- order_status_stage

- order_status_code

退回：

- return_quantity = 退回数量，null填0

数值敏感字段：

当前不要进入算法 clean 主字段。如果保留，只能以 raw_sensitive 前缀保留，并在字段字典中标记 algorithm_usable=false。

例如：

- raw_sensitive_purchase_price

- raw_sensitive_purchase_quantity

- raw_sensitive_purchase_amount

- raw_sensitive_delivery_quantity

- raw_sensitive_delivery_amount

- raw_sensitive_arrival_quantity

- raw_sensitive_arrival_amount

保存：

exports/clean/bs_agent_dingdan_clean.parquet

exports/clean/bs_agent_dingdan_clean_sample.csv

14. 生成 Markdown 数据质量报告

保存：

exports/eda/bs_agent_dingdan_profile.md

报告必须包含：

- 总体行数、列数、采购时间范围

- 唯一标识符结论

- 地区编码一致性结论

- 药品编码与医保编码一致性结论

- 数值字段脱敏破坏程度结论

- 订单状态归类覆盖率

- 医疗机构等级解析覆盖率

- 企业/生产企业字段是否冗余

- 药品类别数量与映射表路径

- 所有制形式数量与映射表路径

- 当前可用于算法的字段列表

- 当前不可用于算法的字段列表

- 待向数据提供者确认的问题列表

四、实现要求

1. Notebook 中尽量使用 pandas，不要引入复杂框架。

2. 所有列名映射不要硬编码在散落的代码里，应集中写到 schema yaml 中。

3. 原始数据不要覆盖，采用 raw -> clean 的方式。

4. 对于需要人工复核的映射，输出 review CSV。

5. 不要做复杂算法，不要启动 L0.5、P_alive、IPI、趋势检测等 detector。

6. 本阶段目标是证明字段是否可靠，以及形成可复用的数据清洗层。

请更新上一版 Notebook 与清洗方案中的“8. 医疗机构等级清洗”口径。

新的口径如下：

1. clean 表中直接使用字段【医疗机构等级】，不再使用【医疗机构详细等级】作为主清洗依据。

2. 【医疗机构详细等级】暂时认为是错误或不可信数据，只保留在 raw/audit 层，不进入 clean 主字段，也不进入算法字段。

3. 对【医疗机构等级】做归一化和数值化编码，输出以下字段：

- hospital_level_raw：原始医疗机构等级

- hospital_level_label：归一化标签

- hospital_level_code：数值编码

4. 初版映射建议：

- 未定级 -> 0

- 一级 -> 1

- 二级 -> 2

- 三级 -> 3

- 其他 -> 9

- 空值/未知 -> -1

5. 注意：

hospital_level_code 虽然是数字，但它本质上是有序类别变量。后续用于分组、排序、同侪对照可以，但建模时不要默认把它当作连续变量。需要在字段字典中注明 semantic_type=ordinal_category。

6. 输出以下报告：

- exports/eda/hospital_level_value_counts.csv

- exports/eda/hospital_level_unmapped_values.csv

- exports/mappings/hospital_level_map.csv

7. 更新：

- docs/data_dictionary/BS_Agent_DingDan字段说明.md

- configs/data_schema/bs_agent_dingdan_schema.yaml

- configs/mappings/hospital_grade_map.yaml

请调整 Notebook 的数据读取与缓存策略，避免强依赖全量 CSV。

新的数据处理策略：

1. SQL Server 是真实数据源。

2. CSV 只用于小样本导出、人工 review 映射表、EDA 汇总结果，不作为百万级主数据处理格式。

3. 原始全量数据如需本地缓存，应优先保存为 Parquet：

- data/raw/BS_Agent_DingDan.parquet

4. 清洗后的数据也应保存为 Parquet：

- data/clean/bs_agent_dingdan_clean.parquet

5. Notebook 支持三种读取模式：

- mode="sql_sample"：从 SQL Server 读取 TOP N 样本，用于快速调试；

- mode="sql_full_to_parquet"：从 SQL Server 分批读取全量数据并写入 Parquet；

- mode="parquet"：从本地 Parquet 读取或懒加载，进行后续 EDA 和清洗。

6. 大部分字段级 EDA 尽量优先在 SQL 侧完成聚合，不要无必要地全表拉到 pandas 中。

7. 如果使用 pandas 从 SQL 读取全量数据，必须使用 chunksize 分批读取，避免一次性读爆内存。

8. 输出 CSV 仅限：

- exports/eda/*.csv

- exports/mappings/*.csv

- exports/clean/bs_agent_dingdan_clean_sample.csv

9. 不要输出全量 fact_order.csv 作为主数据文件。